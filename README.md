# Traffic Sign Recognition with Teachable Machine

Build index:

```bash
./build.sh > index.html
```

This will include all images in `img/` on the webpage.
The html doc is included in `docs/index.html` and is served with github pages.
